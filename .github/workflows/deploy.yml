name: Deploy

# 触发条件：推送到 main 分支时执行
on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest  # 运行环境
    steps:
      - name: Checkout 代码
        uses: actions/checkout@v4  # 拉取代码到运行环境

      - name: 执行部署命令（示例）
        run: |
          echo "开始部署..."
          # 这里添加你的部署命令，如 npm run build、上传到服务器等
